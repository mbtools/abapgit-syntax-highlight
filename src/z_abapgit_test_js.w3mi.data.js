/*
 * Multi-line comment
 */

// Another comment
var variable = "some text";
var variable_2 = 'also text';
var variable_3 = Array( true, false );

/* next line was added */

/* next line was changed */
if (0 == 1) { variable = true; }

/* next line was removed */
if (0 == 1) { variable = true; }

/* eof */
